<h2>Sign Up</h2>

<table>
  <%= form_for @user do |f| %>
    <tr>
      <td><%= f.label :name %></td>
      <td><%= f.text_field :name %></td>
    </tr>
    <%= render partial: 'user_form', locals: {f: f} %>
  <% end %>
</table>
