<div class="comment">
  <p class="comment_body"><%=comment.body%></p>
  <h6>by: <%=link_to "#{comment.user.name}", user_path(comment.user)%> | <%# link_to "reply", parent_child_path(@parent, @child) %></h6>
  <div class="nested_comments">
    <%= render comment.comments %>
  </div>
</div>
